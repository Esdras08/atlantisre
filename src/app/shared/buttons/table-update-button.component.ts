import {Component} from '@angular/core';

@Component({
    selector: 'app-table-update-button',
    template: `<button mat-icon-button color="primary" matTooltip="Modifier l'element">
        <mat-icon>create</mat-icon>
    </button>`
})
export class TableUpdateButtonComponent {
}
